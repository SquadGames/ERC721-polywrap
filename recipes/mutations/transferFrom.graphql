mutation TransferFrom($ddress: String!, $from: String!, $to: String!, $tokenId: BigInt!) {
  transferFrom(
    from: $from
    to: $to
    tokenId: $tokenId
    address: $address
    connection: {
      networkNameOrChainId: $network
    }
  )
}
