mutation SafeTransferFrom($ddress: String!, $from: String!, $to: String!, $tokenId: BigInt!) {
  safeTransferFrom(
    from: $from
    to: $to
    tokenId: $tokenId
    address: $address
    connection: {
      networkNameOrChainId: $network
    }
  )
}
