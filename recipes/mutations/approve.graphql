mutation Approve($address: String!, $to: String!, $tokenId: Int!) {
  approve(
    address: $address
    to: $to
    tokenId: $tokenId
    connection: {
      networkNameOrChainId: $network
    }
  )
}
